---
/**
 * /kiosko/demo.astro - Ruleta Loca Standalone Premium
 * 100% independiente, sin backend
 * Estilo Las Vegas Casino - Mobile First
 */

// Premios hardcodeados para demo
const prizes = [
  { id: '1', name: 'VIAJE GALÁPAGOS', color: '#C41E3A', type: 'travel' },
  { id: '2', name: 'GIFT CARD $100', color: '#1B4D3E', type: 'giftcard' },
  { id: '3', name: 'SIGA INTENTANDO', color: '#2C2C2C', type: 'retry' },
  { id: '4', name: 'GIFT CARD $50', color: '#8B0000', type: 'giftcard' },
  { id: '5', name: 'ASESORÍA GRATIS', color: '#1B4D3E', type: 'service' },
  { id: '6', name: 'SIGA INTENTANDO', color: '#2C2C2C', type: 'retry' },
  { id: '7', name: 'DESCUENTO 20%', color: '#C41E3A', type: 'discount' },
  { id: '8', name: 'CENA PARA 2', color: '#8B0000', type: 'dinner' },
];

// Marcas TDC Ecuador
const cardBrands = [
  { name: 'Visa', color: '#1A1F71' },
  { name: 'Mastercard', color: '#EB001B' },
  { name: 'Diners Club', color: '#004A97' },
  { name: 'Amex', color: '#006FCF' },
];

// URLs de sonidos
const sounds = {
  start: 'https://assets.mixkit.co/active_storage/sfx/2568/2568-preview.mp3',
  spin: 'https://assets.mixkit.co/active_storage/sfx/2003/2003-preview.mp3',
  win: 'https://assets.mixkit.co/active_storage/sfx/2000/2000-preview.mp3',
  lose: 'https://assets.mixkit.co/active_storage/sfx/2018/2018-preview.mp3',
};
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>🎰 Ruleta de Premios - ConsigueTuVisa</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;800;900&display=swap" rel="stylesheet">
</head>
<body>
  <div class="kiosko-container">
    <div class="main-layout">
      <!-- Panel TDC -->
      <aside class="cards-panel">
        <h3 class="cards-title">💳 Tarjetas Participantes</h3>
        <div class="cards-grid">
          {cardBrands.map((card) => (
            <div class="card-brand" style={`--brand-color: ${card.color}`}>
              <span class="card-name">{card.name}</span>
            </div>
          ))}
        </div>
        <p class="cards-hint">Más tarjetas = Más oportunidades</p>
      </aside>

      <!-- Sección Ruleta -->
      <div class="wheel-section">
        <div class="branding">
          <span class="brand-name">ConsigueTuVisa.com</span>
        </div>
        
        <header class="header">
          <h1 class="title">🎰 ¡TE PONEMOS A VIAJAR! 🎰</h1>
          <p class="subtitle">Gira y gana premios increíbles</p>
        </header>

        <div class="wheel-wrapper">
          <div class="lights-ring">
            {Array.from({ length: 24 }).map((_, i) => (
              <div class="light" style={`--angle: ${i * 15}deg; --delay: ${i * 0.08}s`}></div>
            ))}
          </div>

          <div class="pointer">
            <svg viewBox="0 0 50 70" class="pointer-svg">
              <defs>
                <linearGradient id="pointerGold" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" style="stop-color:#FFD700"/>
                  <stop offset="50%" style="stop-color:#FFA500"/>
                  <stop offset="100%" style="stop-color:#FFD700"/>
                </linearGradient>
              </defs>
              <polygon points="25,70 2,8 25,20 48,8" fill="url(#pointerGold)"/>
            </svg>
          </div>

          <div class="wheel" id="wheel">
            <svg viewBox="0 0 400 400" class="wheel-svg">
              <defs>
                <linearGradient id="rimGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" style="stop-color:#FFD700"/>
                  <stop offset="50%" style="stop-color:#B8860B"/>
                  <stop offset="100%" style="stop-color:#FFD700"/>
                </linearGradient>
                <radialGradient id="centerGradient" cx="50%" cy="50%" r="50%">
                  <stop offset="0%" style="stop-color:#C41E3A"/>
                  <stop offset="100%" style="stop-color:#8B0000"/>
                </radialGradient>
              </defs>

              <circle cx="200" cy="200" r="198" fill="none" stroke="url(#rimGradient)" stroke-width="8"/>
              <circle cx="200" cy="200" r="192" fill="#1a1a1a"/>
              <circle cx="200" cy="200" r="188" fill="none" stroke="rgba(255,215,0,0.3)" stroke-width="2"/>

              <g id="segments">
                {prizes.map((prize, i) => {
                  const angle = 360 / prizes.length;
                  const startAngle = i * angle - 90;
                  const startRad = (startAngle * Math.PI) / 180;
                  const endRad = ((startAngle + angle) * Math.PI) / 180;
                  const radius = 183;
                  const x1 = 200 + radius * Math.cos(startRad);
                  const y1 = 200 + radius * Math.sin(startRad);
                  const x2 = 200 + radius * Math.cos(endRad);
                  const y2 = 200 + radius * Math.sin(endRad);
                  const path = `M 200 200 L ${x1} ${y1} A ${radius} ${radius} 0 0 1 ${x2} ${y2} Z`;
                  const midAngle = startAngle + angle / 2;
                  const midRad = (midAngle * Math.PI) / 180;
                  const chars = prize.name.split('');

                  return (
                    <g>
                      <path d={path} fill={prize.color} stroke="rgba(255,215,0,0.5)" stroke-width="2"/>
                      {chars.map((char, ci) => {
                        const charRadius = 170 - (ci * 9);
                        const charX = 200 + charRadius * Math.cos(midRad);
                        const charY = 200 + charRadius * Math.sin(midRad);
                        return (
                          <text
                            x={charX}
                            y={charY}
                            transform={`rotate(${midAngle + 90}, ${charX}, ${charY})`}
                            text-anchor="middle"
                            dominant-baseline="middle"
                            class="prize-char"
                          >
                            {char}
                          </text>
                        );
                      })}
                    </g>
                  );
                })}
              </g>

              <circle cx="200" cy="200" r="45" fill="url(#centerGradient)"/>
              <circle cx="200" cy="200" r="38" fill="none" stroke="url(#rimGradient)" stroke-width="4"/>
              <text x="200" y="200" text-anchor="middle" dominant-baseline="middle" class="center-text">♠</text>
            </svg>
          </div>
        </div>

        <button id="spinBtn" class="spin-btn">🎲 ¡JUGAR!</button>
      </div>

      <!-- Panel Premio -->
      <div id="prizePanel" class="prize-panel hidden">
        <div class="prize-panel-content">
          <button id="closePanel" class="close-btn">✕</button>
          <div class="prize-header">
            <span id="prizeStatus" class="prize-status">¡GANADOR!</span>
          </div>
          <div class="prize-display">
            <div id="prizeIcon" class="prize-icon">🎁</div>
            <div id="prizeName" class="prize-big-name">GIFT CARD $100</div>
          </div>
          <div id="prizeCodeSection" class="prize-code-section">
            <span class="code-label">Tu código:</span>
            <div class="code-box">
              <span id="prizeCode" class="code-value">ABC123</span>
              <button id="copyCode" class="copy-btn">📋</button>
            </div>
          </div>
          <div id="shareSection" class="share-section">
            <p class="share-label">Comparte:</p>
            <div class="share-buttons">
              <button id="shareFb" class="share-btn facebook">f</button>
              <button id="shareWa" class="share-btn whatsapp">w</button>
            </div>
          </div>
          <button id="nextPlayer" class="next-btn">🔄 Siguiente</button>
        </div>
      </div>
    </div>
  </div>


  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    body {
      font-family: 'Montserrat', sans-serif;
      overflow: hidden;
      height: 100vh;
      background: #0a0a0a;
    }

    .kiosko-container {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: radial-gradient(ellipse at center, #1a0a0a 0%, #0a0a0a 100%);
      position: relative;
      padding: 1rem;
    }

    .main-layout {
      display: flex;
      align-items: center;
      gap: 2rem;
      max-width: 1200px;
      width: 100%;
    }

    /* Panel TDC */
    .cards-panel {
      background: linear-gradient(145deg, rgba(30,30,30,0.9), rgba(15,15,15,0.95));
      border: 1px solid rgba(255,215,0,0.3);
      border-radius: 16px;
      padding: 1.5rem;
      width: 200px;
      backdrop-filter: blur(10px);
    }
    .cards-title {
      color: #FFD700;
      font-size: 0.9rem;
      font-weight: 700;
      margin-bottom: 1rem;
      text-align: center;
    }
    .cards-grid {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }
    .card-brand {
      background: var(--brand-color);
      padding: 0.75rem 1rem;
      border-radius: 8px;
      text-align: center;
      transition: transform 0.2s;
    }
    .card-brand:hover { transform: scale(1.05); }
    .card-name {
      color: white;
      font-weight: 700;
      font-size: 0.85rem;
    }
    .cards-hint {
      color: rgba(255,255,255,0.5);
      font-size: 0.7rem;
      text-align: center;
      margin-top: 1rem;
    }

    /* Sección ruleta */
    .wheel-section {
      display: flex;
      flex-direction: column;
      align-items: center;
      flex: 1;
    }

    .branding {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 0.5rem;
      background: rgba(255,215,0,0.1);
      padding: 0.5rem 1.5rem;
      border-radius: 50px;
      border: 1px solid rgba(255,215,0,0.3);
    }
    .brand-name {
      font-size: 1rem;
      font-weight: 700;
      color: #FFD700;
      letter-spacing: 1px;
    }

    .header { text-align: center; margin-bottom: 1rem; }
    .title {
      font-size: 2rem;
      font-weight: 900;
      color: #FFD700;
      text-shadow: 0 0 20px rgba(255,215,0,0.5), 0 0 40px rgba(255,215,0,0.3);
    }
    .subtitle {
      color: rgba(255,255,255,0.8);
      font-size: 1rem;
      font-weight: 600;
      margin-top: 0.25rem;
    }

    .wheel-wrapper {
      position: relative;
      width: 420px;
      height: 420px;
    }

    /* Luces casino */
    .lights-ring { position: absolute; inset: -20px; border-radius: 50%; pointer-events: none; }
    .light {
      position: absolute;
      width: 12px;
      height: 12px;
      background: #FFD700;
      border-radius: 50%;
      top: 50%;
      left: 50%;
      transform: rotate(var(--angle)) translateY(-230px);
      box-shadow: 0 0 15px 5px currentColor;
      animation: blink 0.5s ease infinite alternate;
      animation-delay: var(--delay);
    }
    .light:nth-child(odd) { background: #FF0000; color: #FF0000; }
    .light:nth-child(even) { background: #FFD700; color: #FFD700; }
    .light:nth-child(3n) { background: #FFFFFF; color: #FFFFFF; }
    @keyframes blink {
      from { opacity: 0.4; transform: rotate(var(--angle)) translateY(-230px) scale(0.8); }
      to { opacity: 1; transform: rotate(var(--angle)) translateY(-230px) scale(1); }
    }

    /* Puntero */
    .pointer {
      position: absolute;
      top: -25px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 20;
      filter: drop-shadow(0 5px 10px rgba(0,0,0,0.5));
    }
    .pointer-svg { width: 50px; height: 70px; }

    /* Ruleta */
    .wheel {
      width: 100%;
      height: 100%;
      transition: transform 4s cubic-bezier(0.17, 0.67, 0.12, 0.99);
    }
    .wheel-svg {
      width: 100%;
      height: 100%;
      filter: drop-shadow(0 10px 40px rgba(0,0,0,0.6));
    }
    .prize-char {
      font-family: 'Montserrat', sans-serif;
      font-size: 11px;
      font-weight: 800;
      fill: white;
      text-shadow: 1px 1px 2px rgba(0,0,0,0.8);
    }
    .center-text { font-size: 32px; fill: #FFD700; font-weight: 900; }

    /* Botón GIRAR */
    .spin-btn {
      margin-top: 1.5rem;
      padding: 1rem 4rem;
      font-size: 1.5rem;
      font-weight: 800;
      font-family: 'Montserrat', sans-serif;
      color: #1a1a1a;
      background: linear-gradient(180deg, #FFD700 0%, #FFA500 50%, #FFD700 100%);
      border: none;
      border-radius: 8px;
      cursor: pointer;
      box-shadow: 0 4px 20px rgba(255,215,0,0.4), inset 0 2px 0 rgba(255,255,255,0.3);
      transition: all 0.2s;
      text-transform: uppercase;
      letter-spacing: 2px;
    }
    .spin-btn:hover:not(:disabled) { 
      transform: translateY(-3px); 
      box-shadow: 0 8px 30px rgba(255,215,0,0.6); 
    }
    .spin-btn:disabled { opacity: 0.5; cursor: not-allowed; }

    /* Panel de premio - Desktop: lateral, Mobile: modal */
    .prize-panel {
      width: 320px;
    }
    .prize-panel.hidden { display: none; }
    .prize-panel-content {
      background: linear-gradient(145deg, rgba(30,30,30,0.95), rgba(15,15,15,0.98));
      border: 2px solid rgba(255,215,0,0.4);
      border-radius: 16px;
      padding: 2rem;
      color: white;
      position: relative;
      box-shadow: 0 20px 60px rgba(0,0,0,0.8), 0 0 40px rgba(255,215,0,0.2);
      animation: slideIn 0.5s ease;
    }
    @keyframes slideIn {
      from { opacity: 0; transform: translateX(50px); }
      to { opacity: 1; transform: translateX(0); }
    }

    .close-btn {
      position: absolute;
      top: 0.75rem;
      right: 0.75rem;
      background: rgba(255,255,255,0.1);
      border: none;
      color: white;
      width: 32px;
      height: 32px;
      border-radius: 50%;
      cursor: pointer;
      font-size: 1rem;
    }

    .prize-header { margin-bottom: 1rem; text-align: center; }
    .prize-status {
      background: linear-gradient(135deg, #FFD700, #FFA500);
      color: #1a1a1a;
      padding: 0.5rem 1.5rem;
      border-radius: 4px;
      font-weight: 800;
      font-size: 1rem;
      letter-spacing: 2px;
    }

    .prize-display {
      background: rgba(255,215,0,0.1);
      border: 1px solid rgba(255,215,0,0.3);
      border-radius: 12px;
      padding: 1.5rem;
      text-align: center;
      margin-bottom: 1.5rem;
    }
    .prize-icon { font-size: 4rem; margin-bottom: 0.5rem; }
    .prize-big-name {
      font-size: 1.3rem;
      font-weight: 800;
      color: #FFD700;
      text-shadow: 0 0 10px rgba(255,215,0,0.5);
    }

    .prize-code-section { margin-bottom: 1rem; }
    .code-label { font-size: 0.8rem; color: rgba(255,255,255,0.6); }
    .code-box {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-top: 0.5rem;
    }
    .code-value {
      background: rgba(0,0,0,0.5);
      padding: 0.75rem 1rem;
      border-radius: 8px;
      font-family: monospace;
      font-size: 1.2rem;
      font-weight: 700;
      color: #4ECDC4;
      flex: 1;
      text-align: center;
      border: 1px solid rgba(78,205,196,0.3);
    }
    .copy-btn {
      background: rgba(255,255,255,0.1);
      border: none;
      color: white;
      padding: 0.75rem;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1.2rem;
    }

    .share-section { margin-bottom: 1rem; }
    .share-label { font-size: 0.8rem; color: rgba(255,255,255,0.6); margin-bottom: 0.5rem; }
    .share-buttons { display: flex; gap: 0.5rem; }
    .share-btn {
      flex: 1;
      padding: 0.75rem;
      border: none;
      border-radius: 8px;
      font-weight: 700;
      cursor: pointer;
      font-size: 1rem;
    }
    .share-btn.facebook { background: #1877F2; color: white; }
    .share-btn.whatsapp { background: #25D366; color: white; }

    .next-btn {
      width: 100%;
      padding: 1rem;
      background: linear-gradient(135deg, #C41E3A, #8B0000);
      border: none;
      border-radius: 8px;
      color: white;
      font-weight: 700;
      font-size: 1rem;
      cursor: pointer;
    }

    /* MOBILE: Modal centrado */
    @media (max-width: 900px) {
      .main-layout { 
        flex-direction: column; 
        gap: 1rem;
      }
      .cards-panel { 
        width: 100%; 
        max-width: 400px;
        order: -1;
      }
      .cards-grid {
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: center;
      }
      .card-brand {
        flex: 1;
        min-width: 80px;
        max-width: 100px;
      }
      .wheel-wrapper { 
        width: 320px; 
        height: 320px; 
      }
      .light {
        width: 10px;
        height: 10px;
        transform: rotate(var(--angle)) translateY(-175px);
      }
      @keyframes blink {
        from { opacity: 0.4; transform: rotate(var(--angle)) translateY(-175px) scale(0.8); }
        to { opacity: 1; transform: rotate(var(--angle)) translateY(-175px) scale(1); }
      }
      .title { font-size: 1.5rem; }
      .spin-btn { padding: 0.75rem 2.5rem; font-size: 1.2rem; }
      
      /* Modal en mobile */
      .prize-panel {
        position: fixed;
        inset: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.9);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 100;
        padding: 1rem;
      }
      .prize-panel.hidden { display: none; }
      .prize-panel-content {
        max-width: 350px;
        width: 100%;
        animation: modalIn 0.4s ease;
      }
      @keyframes modalIn {
        from { opacity: 0; transform: scale(0.8); }
        to { opacity: 1; transform: scale(1); }
      }
    }
  </style>


  <script define:vars={{ prizes, sounds }}>
    let isSpinning = false;
    let currentRotation = 0;
    let spinSound, winSound, loseSound, startSound;
    
    const wheel = document.getElementById('wheel');
    const spinBtn = document.getElementById('spinBtn');
    const prizePanel = document.getElementById('prizePanel');
    const closePanel = document.getElementById('closePanel');
    const nextPlayer = document.getElementById('nextPlayer');
    const copyCode = document.getElementById('copyCode');
    const shareFb = document.getElementById('shareFb');
    const shareWa = document.getElementById('shareWa');

    const segmentAngle = 360 / prizes.length;

    // Iconos por tipo de premio
    const prizeIcons = {
      travel: '✈️',
      giftcard: '🎁',
      retry: '🔄',
      service: '📋',
      discount: '💰',
      dinner: '🍽️'
    };
    
    function initSounds() {
      startSound = new Audio(sounds.start);
      startSound.volume = 0.6;
      
      spinSound = new Audio(sounds.spin);
      spinSound.volume = 0.5;
      
      winSound = new Audio(sounds.win);
      winSound.volume = 0.8;
      
      loseSound = new Audio(sounds.lose);
      loseSound.volume = 0.6;
    }

    let soundsReady = false;
    document.addEventListener('click', () => {
      if (!soundsReady) {
        initSounds();
        soundsReady = true;
      }
    }, { once: true });

    spinBtn.addEventListener('click', spin);
    closePanel.addEventListener('click', reset);
    nextPlayer.addEventListener('click', reset);
    
    copyCode.addEventListener('click', () => {
      const code = document.getElementById('prizeCode').textContent;
      navigator.clipboard.writeText(code);
      copyCode.textContent = '✓';
      setTimeout(() => copyCode.textContent = '📋', 2000);
    });

    shareFb.addEventListener('click', () => {
      const text = `¡Gané ${document.getElementById('prizeName').textContent} en ConsigueTuVisa! 🎰✈️`;
      window.open(`https://www.facebook.com/sharer/sharer.php?quote=${encodeURIComponent(text)}`, '_blank');
    });

    shareWa.addEventListener('click', () => {
      const text = `¡Gané ${document.getElementById('prizeName').textContent} en ConsigueTuVisa! 🎰✈️ Código: ${document.getElementById('prizeCode').textContent}`;
      window.open(`https://wa.me/?text=${encodeURIComponent(text)}`, '_blank');
    });

    function spin() {
      if (isSpinning) return;
      
      if (!soundsReady) { initSounds(); soundsReady = true; }
      
      isSpinning = true;
      spinBtn.disabled = true;
      spinBtn.textContent = '🎲 Girando...';

      if (startSound) {
        startSound.currentTime = 0;
        startSound.play().catch(() => {});
      }

      setTimeout(() => {
        if (spinSound) {
          spinSound.currentTime = 0;
          spinSound.play().catch(() => {});
        }
      }, 300);

      const prizeIndex = Math.floor(Math.random() * prizes.length);
      const prize = prizes[prizeIndex];

      const extraSpins = 4 + Math.floor(Math.random() * 2);
      const prizeAngle = prizeIndex * segmentAngle;
      const targetRotation = currentRotation + (extraSpins * 360) + (360 - prizeAngle) - (segmentAngle / 2);
      
      currentRotation = targetRotation;
      wheel.style.transform = `rotate(${targetRotation}deg)`;

      setTimeout(() => {
        if (spinSound) spinSound.pause();
        showResult(prize);
      }, 4200);
    }

    function showResult(prize) {
      const isWinner = prize.type !== 'retry';
      
      if (isWinner && winSound) {
        winSound.currentTime = 0;
        winSound.play().catch(() => {});
      } else if (!isWinner && loseSound) {
        loseSound.currentTime = 0;
        loseSound.play().catch(() => {});
      }

      document.getElementById('prizeStatus').textContent = isWinner ? '¡GANADOR!' : '¡SIGA INTENTANDO!';
      document.getElementById('prizeIcon').textContent = prizeIcons[prize.type] || '🎁';
      document.getElementById('prizeName').textContent = prize.name;
      document.getElementById('prizeCode').textContent = generateCode();

      document.getElementById('prizeCodeSection').style.display = isWinner ? 'block' : 'none';
      document.getElementById('shareSection').style.display = isWinner ? 'block' : 'none';

      prizePanel.classList.remove('hidden');
    }

    function reset() {
      prizePanel.classList.add('hidden');
      isSpinning = false;
      spinBtn.disabled = false;
      spinBtn.textContent = '🎲 ¡JUGAR!';
    }

    function generateCode() {
      return 'CTV' + Math.random().toString(36).substring(2, 8).toUpperCase();
    }
  </script>
</body>
</html>
